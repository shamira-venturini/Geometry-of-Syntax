schema_version: 1

experiment:
  id: exp1a
  seed: 13
  output_dir: behavioral_results/experiment-1/experiment-1a/default_run
  max_items: 2048
  preset: paper_main
  skip_report: false
  skip_stats: false

models:
  - name: gpt2-large
    model_condition: gpt2_large
    device: cuda
    torch_dtype: auto
    batch_size: 8
    local_files_only: false

